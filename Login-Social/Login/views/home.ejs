<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Family Info - Home</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome for icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card shadow">
          <div class="card-body p-5">
            <div class="text-center mb-4">
              <img src="<%= user.photos[0].value %>" alt="Profile Picture" class="rounded-circle img-thumbnail" style="width: 150px; height: 150px; object-fit: cover;">
              <h1 class="mt-3 mb-0 fw-bold">Welcome, <%= user.displayName %>!</h1>
              <p class="text-muted">
                <i class="fas fa-envelope me-2"></i><%= user.emails[0].value %>
              </p>
            </div>
            
            <div class="row g-4 text-center">
              <div class="col-md-4">
                <div class="p-3 bg-primary bg-opacity-10 rounded">
                  <i class="fas fa-users fs-1 text-primary mb-3"></i>
                  <h5>Family Members</h5>
                  <p class="mb-0">Manage your family</p>
                </div>
              </div>
              <div class="col-md-4">
                <div class="p-3 bg-success bg-opacity-10 rounded">
                  <i class="fas fa-calendar-alt fs-1 text-success mb-3"></i>
                  <h5>Events</h5>
                  <p class="mb-0">Upcoming activities</p>
                </div>
              </div>
              <div class="col-md-4">
                <div class="p-3 bg-info bg-opacity-10 rounded">
                  <i class="fas fa-cog fs-1 text-info mb-3"></i>
                  <h5>Settings</h5>
                  <p class="mb-0">Customize your account</p>
                </div>
              </div>
            </div>

            <div class="text-center mt-4">
              <a href="/logout" class="btn btn-outline-danger">
                <i class="fas fa-sign-out-alt me-2"></i>Logout
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS Bundle with Popper -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>